# Полное код-ревью CRM «Детроид»

Дата: 2026-09-07, обновлено 2026-09-08 (второй проход: мёртвый код и чистка, см. раздел 12).
Ревью по состоянию ветки `arena/01a07d21-crmdetroid` (первый проход — коммит `ed8abc9`).
Просмотрены все файлы: `api.php`, `db.php`, `js/app.js`, `js/theme.js`, `index.html`, `ui.html`,
`app.css`, `noscript.css`, `schema.sql`, `.htaccess` (все три), `config.example.php`, `tests/*`.

Шкала: 🔴 критично / 🟠 важно / 🟡 желательно / 🟢 заметка.

---

## 1. Общая оценка

Для «двух PHP-файлов без фреймворка» код **на удивление зрелый**. Видно, что проект прошёл
несколько итераций исправлений безопасности (комментарии в коде честно описывают старые
уязвимости и их фиксы): одноразовые login-токены с HMAC, bcrypt + dummy-hash против тайминг-атак,
лимиты входа по (email, IP) с учётом доверенных прокси, инвалидация сессий через
`token_version` + отпечаток пароля, проверка magic bytes у загрузок, оптимистическая блокировка
через `updated_at`, миграции под `GET_LOCK`. Это уровень выше среднего для самописных CRM.

Главные системные слабости — не отдельные баги, а **масштабируемость подхода**:
монолит из двух гигантских файлов, отсутствие FK в БД, полотно `innerHTML` на клиенте,
отсутствие CI и статического анализа. Плюс несколько конкретных дефектов ниже.

---

## 2. Найденные дефекты (конкретные)

### 2.1 🔴 `save_carrier`: любой сотрудник может редактировать чужого перевозчика — ✅ ИСПРАВЛЕНО
`api.php`, case `save_carrier` (~стр. 758–795). README и `save_direction` декларируют правило:
«переименовать направление **или перевозчика** может тот, кто его добавил, или админ».
`delete_carrier` проверяет `can_manage_ref()`, `save_direction` (rename) проверяет,
а ветка **обновления** в `save_carrier` — нет:

```php
$st = $pdo->prepare('SELECT * FROM crm_carriers WHERE id = ? AND direction_id = ?');
...
// нет can_manage_ref($user, $cur) !
$upd = $pdo->prepare('UPDATE crm_carriers SET name = ?, phone = ?, company = ?, note = ? ...');
```

Любой авторизованный сотрудник может переименовать/затереть карточку чужого перевозчика
(включая заметку). Это нарушение задокументированной модели прав.
**Фикс:** добавить `if (!can_manage_ref($user, $cur)) err('Переименовать перевозчика может тот, кто его добавил, или администратор');`
— либо явно задокументировать в README, что правка полей разрешена всем (но тогда
несимметрично с направлением).

### 2.2 🟠 `save_lead`: обращение к `$row['updated_at']` при `$row === null` + мёртвый `??` — ✅ ИСПРАВЛЕНО
`api.php:987`:

```php
$toName = crm_transfer_lead($pdo, $id, $uid, $toId, $ownerName, $stage, $via, (int) $row['updated_at'] ?? $now);
```

Две проблемы:
1. Приоритет операторов: `(int) $row['updated_at'] ?? $now` парсится как
   `((int) $row['updated_at']) ?? $now` — результат каста никогда не `null`, `?? $now` — мёртвый код.
2. При **создании нового лида с одновременной передачей** (`$row === null`) обращение
   `$row['updated_at']` из-под каста не попадает под null-подавление `??` → warning
   «Trying to access array offset on null» → ваш же `set_error_handler` превращает его
   в `ErrorException` → он не `PDOException` → пробрасывается → **500 вместо ответа**.
   Сейчас UI не создаёт лид с `transferTo` в одном запросе, поэтому баг латентный,
   но прямой вызов API его воспроизводит.

**Фикс:** `$row !== null ? (int) $row['updated_at'] : $now` (и вообще запретить transferTo
для только что созданного лида либо передавать `$now`).

### 2.3 🟠 Документация схемы разъехалась с кодом — ✅ ИСПРАВЛЕНО (шапки обновлены на v13)
`db.php`: `CRM_SCHEMA_VERSION = 13` и есть `crm_migrate_v13()` (индексы `idx_lead_time`,
`idx_carrier_time`, `idx_lead_created`). При этом:
- `schema.sql` в шапке: «schema version 12»;
- README: «Что делает текущая версия схемы (v12)».

Сам файл `schema.sql` индексы v13 содержит (т.е. отстала только шапка и README), но это
демонстрирует главный риск дублирования схемы: **`schema.sql` поддерживается руками** и
неизбежно дрейфует. **Фикс:** генерировать `schema.sql` скриптом из тестовой БД
(`mysqldump --no-data`) или хотя бы добавить проверку в тесты.

### 2.4 🟡 `logout` без CSRF-токена, если в сессии его ещё нет — ✅ ИСПРАВЛЕНО
Для аутентифицированной сессии (`user_id > 0`) проверка CSRF теперь строгая: пустой токен
в сессии или в заголовке — отказ. Ниже — исходное описание.

`api.php`, ветка `logout`: `if ($have !== '' && ...) err('CSRF');` — при пустом
`$_SESSION['csrf']` проверка пропускается. Злоумышленник сторонним POST может разлогинить
пользователя (logout-CSRF). Риск низкий (DoS-неудобство), но проверку честнее сделать строгой.

### 2.5 🟡 Ошибки API всегда отдаются с HTTP 200 — ✅ ИСПРАВЛЕНО
`err()` принимает необязательный `$status`; без него код подбирается по тексту сообщения
(`crm_err_status`: 401 need_login, 403 права/CSRF, 404 не найдено, 405 метод, 409 конфликт
версий, 429 лимиты, 500 внутренние, 400 остальное). Клиент на статус не завязан — совместимо.
Ниже — исходное описание.

`out()/ok()/err()` не выставляют статус (кроме общего catch → 500 и пары мест в `file`).
`Нет прав` → 200, `Сессия истекла` → 200. Клиент завязан на `success:false`/`need_login`,
и это работает, но:
- ломается наблюдаемость: в логах хостинга/прокси всё выглядит как 200;
- сторонние инструменты (curl-мониторинг, fail2ban, uptime-чекеры) не видят проблем.

**Фикс:** `err()` принимает код (`err('Нет прав', 403)`), маппинг: 401 need_login, 403 права,
404 не найдено, 409 оптимистическая блокировка, 422 валидация, 429 лимиты.

### 2.6 🟡 `err('CSRF')` как сообщение о неверном методе — ✅ ИСПРАВЛЕНО
`login` теперь отвечает «Метод не поддерживается: нужен POST» (405) и «Ожидается
Content-Type: application/json» (400) вместо общего «CSRF». Ниже — исходное описание.

`if ($method !== 'POST' || !crm_want_json()) err('CSRF');` (login) и
`if ($method !== 'POST') err('Метод не поддерживается...')` рядом. Сообщение «CSRF» при
неправильном Content-Type дезориентирует при отладке интеграций.

### 2.7 🟡 Мусор в комментариях — след генерации — ✅ ИСПРАВЛЕНО
`js/app.js:147`: «Tab/Shift+Tab**循环** внутри открытой модалки» — китайские иероглифы
в русском комментарии. Вычищено; повторный поиск по CJK-диапазону чист.

### 2.8 🟡 Нет обработки коллизии id — ✅ ИСПРАВЛЕНО
Все точки генерации id используют `crm_new_id($pdo, $prefix, $table)`: до 3 попыток с проверкой
уникальности по таблице (белый список таблиц; при недоступности проверки — id как раньше).
Ниже — исходное описание.

Идентификаторы `l_/c_/a_/d_/k_ + bin2hex(random_bytes(6))` — 48 бит случайности.
Вероятность коллизии мизерная, но при её наступлении `INSERT` упадёт с duplicate key →
пользователь получит «Не удалось сохранить» без повтора. **Фикс:** один retry в цикле
или UUIDv7 (заодно сортируемость).

### 2.9 🟡 `loadActivity()` ходит мимо `Net.req` — ✅ ИСПРАВЛЕНО
`loadActivity` переведён на `Net.req` (обрабатывает `need_login` и ошибки, Toast при сбое);
`_activityCache` ограничен 24 ключами; заодно у всех не-keepalive запросов появился таймаут
20 с (`AbortSignal.timeout`). Ниже — исходное описание.

`js/app.js`: вкладка «Активность» делает голый `fetch` — не обрабатывает `need_login`
(тихо ничего не покажет вместо выхода на форму логина), не показывает ошибки, не
переиспользует логику `as`. Плюс `_activityCache` растёт без ограничения (мелочь).

### 2.10 🟢 `crm_session_gc`: `@filemtime($f) < $limit` при ошибке — ✅ ИСПРАВЛЕНО
`filemtime === false` теперь пропускает файл, а не удаляет его. Ниже — исходное описание.

`filemtime` → `false` → `false < $limit` → `true` → попытка удалить файл, у которого не
прочиталось время. Практически безвредно (файл и так битый), но `(int)@filemtime(...) ?: PHP_INT_MAX`
было бы аккуратнее.

---

## 3. Безопасность (что уже хорошо и что усилить)

### Сделано хорошо ✅
- Login: одноразовые nonce-токены (HMAC + БД, TTL 15 мин), dummy-bcrypt против перечисления
  пользователей по таймингу, лимит 8/(email,IP) и 80/IP за 15 минут, «успех сбрасывает
  счётчик только своей пары (email, IP)».
- Сессии: `use_strict_mode`, HttpOnly, SameSite=Lax, Secure по `crm_is_https()`,
  regenerate_id на login/logout, свой каталог `data/sessions` (обход чужого GC на shared-хостинге),
  idle 8 ч + абсолютный срок 14 дней, отпечаток пароля + `token_version` → мгновенная
  инвалидация при смене пароля/роли.
- CSRF: заголовочный токен + `hash_equals`, GET-мутации запрещены, список read-only actions.
- Загрузки: белый список расширений + анти-двойное-расширение, magic bytes, случайное имя,
  каталог закрыт `.htaccess`, выдача только через `api.php?action=file` с проверкой прав
  и `realpath`-контейнментом, `nosniff`, RFC 5987 для кириллических имён.
- XSS: `esc()` применяется последовательно во всех `innerHTML`-шаблонах (проверял точечно
  все места конкатенации), `safeAttUrl()` — второй слой против `javascript:`-URL,
  CSP `script-src 'self'` без inline, плюс проверка «ui.html не содержит <script>».
- `X-Forwarded-For` доверяется только с явно перечисленных прокси, CIDR-парсер корректный
  (IPv4/IPv6, битовые маски).
- SQL: везде prepared statements; динамические имена таблиц проходят через белые списки
  (`crm_att_urls`, `crm_delete_att_rows`, `crm_insert_comment`).

### Стоит усилить 🔧
1. 🟠 **Права на редактирование перевозчика** — см. дефект 2.1.
2. 🟡 **Rate limit по IP на весь API.** Сейчас лимит 90/мин живёт в сессии — злоумышленник
   с валидной кукой ограничен, но каждая новая сессия = новый лимит; на `?action=file` — 600/мин
   на сессию. Для параноидального режима — счётчик по IP в `crm_login_attempts`-подобной таблице.
3. ✅ **Заголовки** — сделано: `Cross-Origin-Opener-Policy: same-origin` и
   `Cross-Origin-Resource-Policy: same-origin` добавлены в `.htaccess`.
   `includeSubDomains` в HSTS оставлен на усмотрение владельца домена (нужно убедиться,
   что все поддомены живут на HTTPS).
4. ✅ **Argon2id** — сделано: `crm_password_hash()` использует Argon2id, если он собран в PHP
   (иначе bcrypt), а `password_needs_rehash` при логине плавно перехэширует старые пароли.
   Побочный эффект перехэша: остальные сессии пользователя разлогинятся один раз
   (отпечаток пароля в сессии изменится).
5. ✅ **Аудит-лог** — сделано: таблица `crm_audit` (миграция v14), записи о входах,
   создании/изменении/удалении пользователей, смене паролей и передаче лидов;
   просмотр админом через `action=get_audit`, автоочистка записей старше года.
6. 🟢 Ротация пароля БД: в комментарии признаётся, что «пароль БД однажды уже утёк в git».
   История репо squash-нута до 1 коммита — хорошо, но убедитесь, что пароль **сменён**, а не
   только скрыт.
7. 🟢 ZIP/7z принимаются без ограничений содержимого — для внутренней CRM приемлемо,
   но антивирус-скан (clamav на cron) не помешал бы.

---

## 4. Архитектура backend

1. 🟠 **Монолит из 2 файлов** (`api.php` 1343 стр. + `db.php` 1650 стр.). В коде уже есть
   отличный план рефакторинга (TODO #15, #18, #20 в шапках файлов) — его стоит наконец
   выполнить: `actions/*.php`, `http.php`, `security.php`, `files.php`, `migrations.php`.
   Сейчас `db.php` знает про HTTP (`out/err` с `header()`+`exit`) — слой данных не переиспользуем
   из CLI/cron, что сами авторы и признают.
2. 🟠 **`never`-функции (`err()`) внутри бизнес-логики** — поток управления через `exit`
   делает код нетестируемым юнитами: ни одну функцию из `api.php` нельзя вызвать в PHPUnit
   без буферизации вывода. Переход на исключения (`throw new ApiError(403, ...)`) + один
   обработчик — главный шаг к тестируемости.
3. 🟡 **Роутинг**: гигантский `switch` + цепочка `if ($action === ...)` до него. Таблица
   `['save_lead' => [handler, 'POST', 'csrf' => true, 'admin' => false]]` устранит дубли
   логики «read/write actions» и сделает права декларативными.
4. ✅ **Дублирование лид/перевозчик** — сделано (частично): тела add/edit/delete комментариев
   обобщены в `crm_apply_comment_add/edit/delete` (имена таблиц и touch-функция — параметры;
   case-блоки оставлены только для прав и валидации владельца). `crm_lead_comments` /
   `crm_carrier_comments` в `db.php` остаются раздельными — SELECT'ы короткие, обобщение
   дало бы меньше, чем стоило бы читаемости.
5. 🟡 **Смешение форматов ввода** (признано в TODO): `add_comment` — только FormData,
   `edit_comment` — JSON и FormData, остальное — JSON. Унифицировать.
6. 🟢 `applications_count` — денормализация с ручной синхронизацией (`crm_sync_lead_apps_count`).
   Работает, но триггер или полный отказ (COUNT на лету, есть индекс `idx_lead`) проще.

## 5. База данных

1. 🟠 **Нет FOREIGN KEY** — целостность держится на дисциплине кода + `integrity_check`
   (по сути ручной fsck). Все удаления каскадируются вручную в `crm_purge_*`. InnoDB уже
   используется, id-типы совместимы — FK с `ON DELETE CASCADE` для
   `comments→leads`, `attachments→comments`, `lead_apps→leads`, `carriers→directions`
   убрали бы целый класс багов и половину `crm_purge_*`. (Понимаю осознанность решения —
   на shared-хостинге FK иногда боятся, но MySQL 8 их отлично тянет.)
2. 🟠 **Этап лида хранится строкой-именем** (`crm_leads.stage VARCHAR(80)` ↔ `crm_stages.name`).
   Отсюда вся хрупкая механика `crm_stage_renames` («ровно один исчез и один появился — считаем
   переименованием»), `UPDATE ... NOT IN (...)` при сохранении этапов и потеря этапа при передаче
   лида. Ссылка `stage_id` на `crm_stages.id` устранила бы эвристику полностью.
3. 🟡 `save_stages` делает `DELETE всех этапов + INSERT заново` — id этапов не стабильны
   (сейчас на них никто не ссылается, но это блокирует п.2).
4. 🟡 Времена — `BIGINT` миллисекунды. Работает, но `DATETIME(3)` дал бы читаемость в
   phpMyAdmin и функции дат без `FROM_UNIXTIME(x/1000)` (см. п.6.3 про активность).
5. 🟢 Миграции: линейные, идемпотентные, под `GET_LOCK`, с честным комментарием про
   нетранзакционность DDL в v10 и сохранением сырых значений в `*_raw` — образцово для
   самописа. Единственное: `crm_run_migrations` прогоняет **все** миграции с нуля даже когда
   версия 12→13 (каждая сама проверяет "уже применено" через information_schema — это
   десятки лишних запросов на первый запрос после деплоя; терпимо, но версионный switch быстрее).

## 6. Производительность

1. 🟠 **Нет пагинации**: `get_data` отдаёт **все** лиды пользователя каждые 15–30 секунд
   каждой открытой вкладке. Хэш-механизм (`unchanged: true`) отлично экономит трафик при
   отсутствии изменений, но при любом изменении сериализуется вся доска. До ~1–2 тыс. лидов
   на пользователя — нормально; дальше нужны пагинация/дельты (`updated_since=<ts>`).
2. 🟡 **Поиск `LIKE '%...%'`** по title/inn — full scan (индексы не работают по ведущему `%`).
   Для роста: FULLTEXT-индекс по `title` + отдельное точное совпадение по нормализованному ИНН.
3. ✅ `crm_client_activity` — сделано: фильтр года заменён на саргируемый диапазон
   `a.created_at >= :jan1_ms AND a.created_at < :nextJan1_ms`.
4. ✅ **`.htaccess`: сжатие и кэширование статики** — сделано: `mod_deflate` для
   text/css/js/json/svg и `mod_expires` на 7 дней для css/js/svg. Срок сознательно не год:
   версионирование ручное (`?v=fixNN`), и забытая версия при годовом кэше означала бы
   неделю → год залипшей статики. При переходе на hash в имени файла срок можно поднять.
5. 🟢 На каждый запрос — `SELECT v FROM crm_meta WHERE k='schema'` (crm_boot). Дешево, ок.
6. 🟢 Подзапросы-COUNT в списках направлений/перевозчиков — N+1 внутри одного SQL, для
   справочника это нормально.

## 7. Frontend (js/app.js, ui.html, app.css)

### Хорошо ✅
- Дисциплина `esc()` во всех шаблонах, делегирование событий через `data-action`,
  оптимистическая блокировка с `_editRev`, цепочки сохранений (`_leadSaveChain`) против
  гонок автосейва, adaptive polling (15с → 30с при простое), `keepalive` в `beforeunload`,
  focus-trap и aria-атрибуты в модалках, защита от автозаполнения пароля в таблице
  сотрудников (детект ручного ввода) — редкая внимательность.

### Слабые места 🔧
1. 🟠 **2291 строка в одном файле без модулей.** `Store`, `Net`, `UI`, роутер, канбан,
   формы, файлы, активность — всё в глобальной области. Даже без сборщика можно перейти на
   нативные ES-модули (`<script type="module">`): `net.js`, `store.js`, `board.js`,
   `lead.js`, `routes.js`, `users.js`, `activity.js`. Это же уберёт хак
   в e2e-тесте, который дописывает `window.Store=Store...` к исходнику.
2. 🟠 **Рендер через innerHTML-полотна.** Помимо читаемости, это причина потери состояния
   (весь лог перерисовывается при каждом изменении, редактор восстанавливается вручную
   через `UI.editingCommentId`). Минимальный шаг — `<template>`-элементы в ui.html вместо
   строковых шаблонов; максимальный — лёгкая библиотека (lit-html/preact, ~4–10 КБ) без
   изменения бэкенда.
3. 🟡 **Глобальный флаг `UI.formDirty` один на все формы** (лид и перевозчик различаются
   только по `UI.carrierId`) — уже сейчас в `switchView/openRoute/openCarrier` по 6 строк
   «сохрани то, потом это». Состояние «что редактируем» просится в объект.
4. ✅ `withLock` + зависший запрос — сделано: в `Net.req` добавлен `AbortSignal.timeout(20000)`
   для всех не-keepalive запросов; зависший запрос теперь завершается ошибкой, и лок
   отпускается штатно.
5. 🟡 Дубли plural-функций: `plural()`, `carrierWord()`, `appsWord()` — три реализации
   одного склонения. ✅ ИСПРАВЛЕНО: `carrierWord`/`appsWord` стали обёртками над `plural()`.
6. ✅ `loadActivity` мимо `handleLogoutUI` — сделано, см. 2.9.
7. 🟢 Телефонная маска жёстко заточена под +7/8 — для международных перевозок может мешать
   (маска пропускает `+` не-РФ, но UX спорный).
8. 🟢 A11y: drag-n-drop канбана недоступен с клавиатуры (перенос этапа возможен только
   мышью; карточку можно двигать кнопками этапов в карточке — уже неплохо). Тостам не хватает
   `role="status"`/`aria-live`.
9. 🟢 Мобильность: `body { overflow: hidden }` + 4 media-query — планшет ок, телефон
   «терпимо». Если менеджеры работают с телефона, нужен отдельный проход по UX.

## 8. Тесты и CI

1. ✅ **CI — сделано:** `.github/workflows/ci.yml`, 4 джоба на каждый push/PR:
   `lint` (php -l, node --check, bash -n), `unit` (`tests/unit.php`),
   `static` (PHPStan level 6 + ESLint), `smoke` (`services: mysql:8` + `php -S` +
   все ~50 проверок `tests/api-smoke.sh` против настоящей БД).
   Уже окупился первым же прогоном: smoke в CI поймал регрессию передачи лида —
   `save_lead` передавал в `crm_transfer_lead` старый `updated_at`, хотя UPDATE выше
   уже поставил новый, и передача существующего лида всегда падала
   «Карточка изменена в другом месте» (исправлено; заодно «Сотрудник не найден»
   теперь проверяется явно, а не угадывается по $row).
2. ✅ **Юнит-тесты чистых функций** — сделано: `tests/unit.php` (без PHPUnit — обычный
   PHP-скрипт с табличными кейсами, exit code 1 при провале; 80+ проверок). Покрыты
   `crm_parse_money`, `crm_legacy_money`, `crm_money_out`, `crm_ip_in_list`,
   `crm_stage_renames`, `crm_upload_name`, `crm_short_filename`, `crm_like_pat`,
   `crm_err_status`, `crm_reserved_user_name`, `crm_is_sys_comment`.
   Запуск: `php tests/unit.php` — БД и HTTP не нужны.
3. 🟡 Smoke-тесты пишут в общую БД стенда и полагаются на заранее созданных
   пользователей (`ivan@x.ru`, «Пётр Сидоров» для e2e) — сделать сетап самодостаточным
   (e2e сам создаёт своих пользователей через API админа, как это делает smoke).
4. ✅ **Статанализ — сделано:** PHPStan level 6 (`phpstan.neon` + `tests/phpstan-bootstrap.php`,
   константы конфига в `dynamicConstantNames`) и ESLint flat config (`eslint.config.mjs`).
   Оба проходят чисто; найденное вычищено: mixed-типы у `strv`/`intv`/`crm_money_out`/
   `crm_parse_money`, мёртвый `?? ''` у cookie-domain, двойной `crm_user_by_id` в
   `action=file`, 15 case-блоков без `{}` (утечка `const` между case), неиспользуемые
   переменные в `app.js` и `e2e-jsdom.mjs`. PHPStan поймал бы дефект 2.2 автоматически.

## 9. Процессы и репозиторий

1. 🟡 История squash-нута до одного коммита «Delete uploads/1» — история изменений и
   авторство утрачены; дальше стоит вести нормальные атомарные коммиты.
2. 🟡 Частично закрыто: появился `package.json` (ESLint как devDependency);
   `composer.json` по-прежнему нет — PHPStan ставится в CI через `setup-php`.
3. 🟡 Деплой — ручная заливка файлов по списку из README («не перезаписывайте config.php,
   uploads/, data/»). Одна ошибка — потеря вложений. Минимум: rsync-скрипт с `--exclude`,
   лучше — деплой по git + симлинки на shared-каталоги.
4. 🟡 Бэкапы: README требует «перед обновлением снимите дамп руками». Cron с
   `mysqldump | gzip` + ротация (на SpaceWeb cron есть) — час работы.
5. 🟢 `.gitignore`, `.htaccess`-защиты, `robots.txt` — образцово.
6. 🟢 Нет LICENSE — для внутреннего проекта ок, но стоит зафиксировать намеренно.

---

## 10. Приоритетный план действий

| # | Действие | Усилие | Эффект |
|---|---|---|---|
| 1 | ✅ Фикс прав в `save_carrier` (дефект 2.1) | сделано | закрывает нарушение модели прав |
| 2 | ✅ Фикс `$row['updated_at']` в save_lead (2.2) | сделано | убирает латентный 500 |
| 3 | ✅ CI: lint + unit + статанализ + smoke с MySQL на каждый push | сделано | регрессии ловятся сами |
| 4 | ✅ PHPStan + ESLint, находки вычищены | сделано | класс ошибок 2.2 исчезает |
| 5 | ✅ Юнит-тесты на чистые функции (`tests/unit.php`) | сделано | защита самой рискованной логики |
| 6 | ✅ HTTP-коды в `err()` + строгий CSRF на logout | сделано | наблюдаемость, гигиена |
| 7 | ✅ mod_deflate + кэширование статики (hash-версии — позже) | сделано | скорость загрузки |
| 8 | Распил api.php/db.php по плану из их же TODO | 2–3 дня | поддерживаемость |
| 9 | ES-модули на клиенте, распил app.js | 2–3 дня | поддерживаемость |
| 10 | FOREIGN KEY + `stage_id` вместо имени этапа | 2 дня + миграция | целостность данных |
| 11 | Автобэкапы по cron, деплой-скрипт | 0.5 дня | защита данных |
| 12 | Дельта-синхронизация/пагинация get_data | по мере роста | масштабируемость |

---

## 11. Второй проход (2026-09-08): мёртвый код и чистка

Методика: сверка всех `data-action` в шаблонах с ветками обработчика; проверка каждой из
~110 JS-функций и всех 130 PHP-функций на использование; сверка CSS-классов с разметкой
(ui.html + index.html + строковые шаблоны app.js); сверка id в разметке со ссылками из
JS/CSS/тестов; поиск не-ASCII мусора.

### Найдено и исправлено ✅

1. **Битый обработчик в поиске дашборда** (баг, не только мёртвый код).
   Результаты поиска на дашборде рендерились с `data-action="open-lead"`, а ветки
   `open-lead` в switch **нет** — клик срабатывал только косвенно, через `href="#lead/…"`
   и hash-роутер, из-за чего выпадающий список не закрывался и найденный лид не открывался
   как положено. Заменено на существующий `open-search-lead`; `closeSearchDrop()` теперь
   закрывает и дашбордный список.
2. **`#k-id` — мёртвое скрытое поле.** `<input type="hidden" id="k-id">` в модалке
   перевозчика только очищался при открытии и никогда не читался (`submit-carrier` всегда
   создаёт нового, редактирование идёт через карточку). Поле и его очистка удалены.
3. **Мёртвый CSS-блок `.dropdown*`** (6 правил + 3 правила тёмной темы) — классов нет
   ни в разметке, ни в JS-шаблонах. Остаток от старого меню. Удалён.
4. **Мёртвый CSS `.carrier-note`** — класс нигде не используется (заметка рендерится
   инпутом `#cf-note` с классом `form-input`). Удалён.
5. **`tests\.sh` в `.htaccess`** — файла `tests.sh` не существует (тесты лежат в `tests/`,
   каталог закрыт отдельным правилом). Мёртвый паттерн заменён на актуальный
   `CODE_REVIEW\.md` (новый файл не должен читаться с сайта) в обеих ветках FilesMatch
   и в RewriteRule.
6. **e2e-тест проверял несуществующий id** — `#user-name` вместо реального
   `#user-display-name`; проверка всегда «проходила» по запасному условию `|| !!user2?.name`,
   т.е. ничего не проверяла. Исправлен селектор.
7. **Иероглифы `循环` в комментарии** (дефект 2.7) — вычищено.
8. **Дубли plural-логики** — `carrierWord`/`appsWord` сведены к `plural()` (п. 7.5).
9. **Рассинхрон версий схемы v12/v13** (дефект 2.3) — шапка `schema.sql` и README
   обновлены на v13, в README дописан пункт про индексы v13.
10. **`.tile-desc` — мёртвый CSS** после редизайна плиток: класс удалён вместе со старым
    блоком стилей при переходе на Odoo-плитки (описания в новых плитках нет).

### Проверено — мёртвого кода НЕ обнаружено 🟢

- **PHP:** все 130 функций `api.php`/`db.php` используются (включая цепочку миграций
  v4–v13 — они вызываются из `crm_run_migrations`). Недостижимых веток в switch нет,
  `default:` достижим.
- **JS:** все ~110 функций вызываются; все 48 `case`-веток обработчика соответствуют
  реальным `data-action` в разметке/шаблонах (после фикса №1); `add-stage` и `set-stage`
  вешаются динамически из `renderBoard`/`renderDetailStages` — живые.
- **CSS:** из 181 класса неиспользуемых после чистки не осталось (сверка по трём
  источникам разметки).
- **id в разметке:** все на что-то ссылаются (после фикса №2 и №6).
- **`escAttr = esc`** — выглядит как дубль, но это осознанный алиас с комментарием
  (семантика «для атрибутов»), используется в `safeAttUrl`. Оставлен.
- **`arguments[3]` в `Net.req`** — нестандартно (4-й параметр без объявления), но
  используется всеми вызовами с `extra`. Работает; при рефакторинге заменить на
  именованный параметр.
- **`crm_migrate_v9`** добавляет колонку `margin` как VARCHAR, которую v10 тут же
  конвертирует в DECIMAL — выглядит избыточно, но нужно для баз, замерших между
  версиями. Не мёртвый код.

### Осталось (не мёртвый код, перенесено в план)

- `Store.viewUserName = 'Сотрудник'` — фолбэк, когда коллега не найден в кэше: живой код.
- ✅ `_activityCache` — ограничен 24 ключами (п. 2.9).
- ✅ Дублирование лид/перевозчик в PHP (add/edit/delete comment) — обобщено в
  `crm_apply_comment_*` (п. 4.4).

---

## 12. Четвёртый проход (2026-09-08, после фиксов третьего): полное перечитывание

Перечитаны целиком api.php (1390 стр.), db.php (1759 стр.), js/app.js (2317 стр.), ui.html,
index.html, .htaccess, schema.sql, config.example.php, tests/. Проверки: `php -l` обоих файлов —
чисто; `node --check app.js` — чисто; `tests/unit.php` — все проверки проходят; дублей id
в ui.html нет; inline-стилей и `<script>` в ui.html нет (CSP-совместимо); v13/v14 в schema.sql
и миграциях синхронны; tests/ и data/ закрыты в .htaccess.

### Проверено — фиксы третьего прохода корректны ✅
- HTTP-коды: `crm_err_status` покрывает все типовые сообщения, клиент от кодов не зависит.
- Строгий CSRF на logout: пустой токен при user_id>0 отклоняется.
- `crm_new_id`: TOCTOU-гонка между SELECT и INSERT осознанно прикрыта самим PK (комментарий на месте).
- `crm_apply_comment_*`: имена таблиц приходят только литералами из call-site'ов — интерполяция безопасна;
  права (can_edit/can_delete/владелец) остались в case-блоках, как и было.
- Аудит: `crm_audit` не роняет запрос, GC старше года, get_audit — только админ, limit ограничен.
- `get_activity` через GET + диапазон created_at; `as` не смешивается со `Store.viewUserId`.
- schema.sql: crm_audit и индексы v13 (idx_lead_time, idx_carrier_time, idx_lead_created) на месте.

### Новые находки

#### 12.1 🟠 Таймаут 20 с в Net.req обрывает большие загрузки файлов — ✅ ИСПРАВЛЕНО
Для FormData-запросов (загрузки файлов) таймаут поднят до 180 с; остальные — 20 с.
Ниже — исходное описание.
`js/app.js`: `AbortSignal.timeout(20000)` применяется ко **всем** не-keepalive запросам,
включая FormData-загрузки (`add_comment`, `add_carrier_comment`, `edit_*_comment`).
До 8 файлов по 5 МБ = до 40 МБ; на медленном аплинке (типичный мобильный 2–5 Мбит/с
вверх) передача занимает минуты — запрос оборвётся на 20-й секунде с «Сбой сети»,
хотя раньше дошёл бы. Регрессия от фикса «зависший запрос держит withLock».
**Фикс:** для `isFormData === true` таймаут не ставить или поднять до 120–180 с.

#### 12.2 🟠 Argon2id вернул перечисление пользователей по таймингу — ✅ ИСПРАВЛЕНО
`crm_dummy_hash()` в db.php: dummy генерируется тем же алгоритмом, что боевые хэши
(`crm_password_algo`), один раз на процесс, со случайным паролем. Покрыто юнит-тестами
(`password_needs_rehash(dummy, algo) === false`). Ниже — исходное описание.
`api.php` (login): dummy-хэш — **bcrypt** cost 10 (~60–100 мс), а реальные пользователи после
плавной миграции получают **Argon2id** (при дефолтных параметрах PHP — заметно другое время
проверки). Несуществующий e-mail → password_verify(bcrypt), существующий → password_verify(argon2id):
разница измерима удалённо, и защита от перечисления, ради которой dummy заводился, ослабла.
**Фикс:** генерировать dummy тем же алгоритмом, что и боевые хэши, — статически один раз:
`static $dummy = null; if ($dummy === null) $dummy = crm_password_hash('dummy-timing-guard');`
(один лишний хэш на процесс — приемлемо; либо кэшировать в data/ как csrf_secret).

#### 12.3 🟡 logout: неверный метод по-прежнему отвечает «CSRF» — ✅ ИСПРАВЛЕНО
Теперь «Метод не поддерживается: нужен POST» (405), как в login. Ниже — исходное описание.
В login сообщения разделили («нужен POST» → 405), а в logout осталось
`if ($method !== 'POST') err('CSRF')` → 403. Мелкая непоследовательность из того же
семейства, что исправленный п. 2.6.

#### 12.4 🟡 js/theme.js кэшируется на 7 дней без версии — ✅ ИСПРАВЛЕНО
Добавлен `?v=fixNN` (теперь три точки версии в index.html: строки 7, 10, 75).
Ниже — исходное описание.
`index.html` подключает `js/theme.js` без `?v=`, а `.htaccess` теперь ставит
`Expires: 7 days` на весь application/javascript. Если theme.js когда-нибудь изменится,
клиенты неделю будут получать старую копию. Пока файл стабилен (3 строки) — не горит,
но при правке легко забыть. **Фикс:** добавить `?v=fixNN` и включить файл в чек-лист версии.

#### 12.5 🟢 move_lead: клиент шлёт поле `from`, сервер его игнорирует — ✅ ИСПРАВЛЕНО
Поле убрано из обоих вызовов (set-stage и drag-n-drop). Ниже — исходное описание.
`js/app.js` дважды (set-stage и drop) передаёт `from: lead.stage` в move_lead — сервер
это поле не читает (берёт этап из БД). Мёртвый груз в payload; убрать при случае.

#### 12.6 🟢 В аудит не пишется logout — ✅ ИСПРАВЛЕНО
`crm_audit(..., 'logout', email)` в ветке logout, до очистки сессии. Ниже — исходное описание.
Входы логируются, выходы — нет. Для разборов инцидентов полезно видеть и то и другое;
одна строка `crm_audit($pdo, ...)` в ветке logout (до session_destroy, пока жив user_id).

#### 12.7 🟢 beforeunload-сохранение с keepalive ограничено ~64 КБ
Браузеры отклоняют keepalive-fetch с телом больше 64 КБ. Форма лида обычно меньше,
но комментарий в 20 000 символов кириллицей (~40 КБ в JSON) + поля подходит к пределу.
Крайний случай; известное ограничение платформы — просто зафиксировано.

#### 12.8 🟢 api-smoke.sh не проверяет HTTP-статусы — ✅ ИСПРАВЛЕНО
Добавлен раздел 13 в smoke-тесты: 401 без сессии, 403 чужая доска, 404 несуществующий лид,
405 GET-мутация, 200 успех (`curl -w '%{http_code}'`). Ниже — исходное описание.
После перехода err() на честные коды в smoke-тестах можно дёшево добавить
`curl -w '%{http_code}'`-проверки (401 без сессии, 403 на чужое, 404 на несуществующее) —
закрепит новый контракт.

#### Осмотрено и признано нормальным (чтобы не перепроверять в следующий раз)
- Сжатие JSON (deflate) + секрет (csrf) в ответе — теоретически BREACH-профиль, но в ответах
  с токеном нет отражённого пользовательского ввода; практический риск ничтожен.
- `$leadRow['inn'] ?? ''` после повторного crm_lead_for_user в delete_lead_app: `??`
  подавляет warning на null — 500 не будет.
- `crm_anon_throttled`/`crm_login_throttled`: нормализация пустого IP в '0.0.0.0' согласована
  во всех трёх функциях (fail/ok/throttled).
- Лимитер в сессии инкрементится до session_write_close — записи не теряются.
- `_activityCache`: FIFO-вытеснение по первому ключу; фактический потолок ~25 ключей — ок.
- Два document-level click-обработчика закрытия дропдаунов (доска и дашборд) — дубль
  осознанный, конфликтов нет.
- `Header always unset Server` не убирает заголовок Apache полностью (нужен ServerTokens
  в httpd.conf, на shared-хостинге недоступен) — косметика, не дефект.

---

## 13. Резюме

Проект в хорошей форме для своего класса: продуманная безопасность, честные комментарии,
рабочие тесты, аккуратные миграции. Оба критичных дефекта первого прохода (права на
перевозчика, null-обращение в save_lead) исправлены; вторым проходом вычищен весь
найденный мёртвый код (битый обработчик поиска дашборда, поле #k-id, CSS .dropdown*/
.carrier-note, паттерн tests.sh в .htaccess, битый селектор в e2e, дубли plural),
синхронизирована документация схемы. Третьим проходом (2026-09-08) закрыты все жёлтые
дефекты раздела 2 (HTTP-коды ошибок, строгий CSRF на logout, раздельные ошибки login,
retry коллизий id, `loadActivity` через `Net.req`, GC сессий), усилена безопасность
(COOP/CORP-заголовки, Argon2id с плавным перехэшем, аудит-лог `crm_audit`, схема v14),
производительность (саргируемый фильтр года, deflate + expires, таймаут fetch),
дедуплицированы комментарии лид/перевозчик и добавлены юнит-тесты чистых функций
(`tests/unit.php`). Осталась планомерная работа по инфраструктуре качества (CI, статанализ)
и постепенный распил монолитов, план которого авторы уже сами записали в TODO. Самый
большой стратегический риск — не код, а процессы: ручной деплой, ручные бэкапы и
отсутствие автоматических проверок.
